<!--<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cutting Edge Gym</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* Reset default margin and padding */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f9f9f9;
            color: #000; /* Default text color changed to black */
        }

        /* Header styles */
        header.hero {
            height: 100vh;
            background-image: url('https://i.imghippo.com/files/uCXbc1715265686.jpg');
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #fff; /* Header text color (white) */
            text-align: center;
            padding: 20px;
        }

        nav {
            margin-top: 20px;
        }

            nav ul {
                list-style: none;
                padding: 0;
            }

                nav ul li {
                    display: inline-block;
                    margin-right: 20px;
                }

                    nav ul li a {
                        text-decoration: none;
                        color: #fff;
                        font-weight: bold;
                        transition: color 0.3s ease;
                    }

                        nav ul li a:hover {
                            color: #e74c3c;
                        }

        .hero-content {
            max-width: 600px;
        }

        .hero h1 {
            font-size: 3.5em; /* Increase h1 font size */
            font-weight: bold;
            margin-bottom: 20px;
            color: #e74c3c; /* Change h1 text color to black */
        }

        .button {
            display: inline-block;
            padding: 12px 32px;
            background-color: #e74c3c;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

            .button:hover {
                background-color: #c0392b;
            }

        /* About section styles */
        .about {
            text-align: center;
            padding: 80px 20px;
            background-color: #ecf0f1;
        }

        .about-content {
            max-width: 600px;
            margin: 0 auto;
        }

        .about h2 {
            font-size: 3.0em; /* Increase h2 font size */
            margin-bottom: 30px;
            color: #000; /* Change h2 text color to black */
        }

        .about p {
            font-size: 2.2em; /* Increase paragraph font size */
            line-height: 1.6;
            color: #000;
        }

        /* Footer styles */
        footer {
            background-color: #2c3e50;
            color: #000;
            text-align: center;
            padding: 30px 0;
        }

        /* Chat box styles */
        .chat-box {
            width: 300px; /* Set desired width */
            height: 200px; /* Set desired height */
            position: fixed;
            top: 20px; /* Adjust distance from top */
            left: 20px; /* Adjust distance from left */
            background-color: #fff;
            padding: 10px;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
            z-index: 999;
            overflow-y: scroll; /* Enable vertical scrolling */
            border: 1px solid #ddd; /* Add a border for clarity */
        }

            .chat-box.open {
                transform: translateY(0);
            }

        .chat-message {
            margin-bottom: 10px;
        }

            .chat-message .sender {
                font-weight: bold;
                color: #333;
            }

            .chat-message .timestamp {
                font-size: 0.8em;
                color: #999;
            }

        .input-box {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 20px;
        }

            .input-box input[type="text"] {
                flex: 1;
                padding: 10px;
                margin-right: 10px;
                border: 1px solid #ccc;
                border-radius: 5px;
                font-size: 16px; /* Adjust font size */
            }

            .input-box button {
                padding: 10px 20px;
                background-color: #e74c3c;
                color: #fff;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                font-size: 16px; /* Adjust font size */
            }

                .input-box button:hover {
                    background-color: #c0392b;
                }
    </style>
</head>
<body>

    <header class="hero">
        <nav>
            <ul>
                <li><a href="#">Home</a></li>
                <li><a href="Class/Index">Classes</a></li>
                <li><a href="Gym/Index">Employees</a></li>
                <li><a href="Contact/Index">Contact</a></li>
            </ul>
        </nav>

        <div class="hero-content">
            <h1>Welcome</h1>
            <p>Your journey to a stronger, healthier you starts here.</p>
            <a href="#" class="button">Get Started</a>
        </div>
    </header>

    <section class="about">
        <div class="about-content">
            <h2>About Us</h2>
            <p>At Cutting Edge Gym, we're committed to pushing boundaries and achieving fitness excellence. Join us and discover the difference.</p>
        </div>
    </section>

    <footer>
        <p>&copy; 2024 Cutting Edge Gym</p>
    </footer>-->
@*Chat box section -->
    <div class="chat-box" id="chatBox">
        <div class="chat-message">
            <span class="sender">John:</span>
            <span>Hello! If you have any questions, please click on the Contact section. We will get back to you as soon as possible.</span>
            <span class="timestamp">12:30 PM</span>
        </div>-->*@
<!-- Add more chat messages here -->
<!--</div>-->
<!--< Chat input form -->
<!--<form class="input-box" onsubmit="return sendMessage();">
                    <input type="text" id="chatInput" placeholder="Type your message...">
                    <button type="submit">Send</button>
                </form>

                <script>// Function to display chat box after delay
                    window.onload = function () {
                        setTimeout(function () {
                            document.getElementById('chatBox').classList.add('open');
                        }, 1000); // Adjust delay (in milliseconds) as needed
                    };

                    // Function to send message and display in chat box
                    function sendMessage() {
                        var input = document.getElementById('chatInput').value;
                        if (input.trim() !== '') {
                            var chatBox = document.getElementById('chatBox');
                            var newMessage = document.createElement('div');
                            newMessage.className = 'chat-message';
                            newMessage.innerHTML = '<span class="sender">You:</span><span>' + input + '</span><span class="timestamp">' + getCurrentTime() + '</span>';
                            chatBox.appendChild(newMessage);
                            document.getElementById('chatInput').value = ''; // Clear input field after sending message
                        }
                        return false; // Prevent form submission
                    }

                    // Function to get current time in HH:mm format
                    function getCurrentTime() {
                        var now = new Date();
                        var hours = now.getHours().toString().padStart(2, '0');
                        var minutes = now.getMinutes().toString().padStart(2, '0');
                        return hours + ':' + minutes;
                    }</script>

            </body>
            </html>-->










<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cutting Edge Gym</title>
    <style>
        /* Reset default margin and padding */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f9f9f9;
            color: #000; /* Default text color changed to black */
        }

        /* Header styles */
        header.hero {
            height: 100vh;
            background-image: url('https://i.imghippo.com/files/uCXbc1715265686.jpg');
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #fff; /* Header text color (white) */
            text-align: center;
            padding: 20px;
        }

        nav {
            margin-top: 20px;
        }

            nav ul {
                list-style: none;
                padding: 0;
            }

                nav ul li {
                    display: inline-block;
                    margin-right: 20px;
                }

                    nav ul li a {
                        text-decoration: none;
                        color: #fff;
                        font-weight: bold;
                        transition: color 0.3s ease;
                    }

                        nav ul li a:hover {
                            color: #e74c3c;
                        }

        .hero-content {
            max-width: 600px;
        }

        .hero h1 {
            font-size: 3.5em; /* Increase h1 font size */
            font-weight: bold;
            margin-bottom: 20px;
            color: #e74c3c; /* Change h1 text color to black */
        }

        .button {
            display: inline-block;
            padding: 12px 32px;
            background-color: #e74c3c;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

            .button:hover {
                background-color: #c0392b;
            }

        /* About section styles */
        .about {
            text-align: center;
            padding: 80px 20px;
            background-color: #ecf0f1;
        }

        .about-content {
            max-width: 600px;
            margin: 0 auto;
        }

        .about h2 {
            font-size: 3.0em; /* Increase h2 font size */
            margin-bottom: 30px;
            color: #000; /* Change h2 text color to black */
        }

        .about p {
            font-size: 2.2em; /* Increase paragraph font size */
            line-height: 1.6;
            color: #000;
        }

        /* Footer styles */
        footer {
            background-color: #2c3e50;
            color: #000;
            text-align: center;
            padding: 30px 0;
        }

        /* Chat box styles */
        .chat-box {
            width: 300px; /* Set desired width */
            height: 200px; /* Set desired height */
            position: fixed;
            top: 20px; /* Adjust distance from top */
            left: 20px; /* Adjust distance from left */
            background-color: #fff;
            padding: 10px;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
            z-index: 999;
            overflow-y: scroll; /* Enable vertical scrolling */
            border: 1px solid #ddd; /* Add a border for clarity */
            transform: translateY(-100%); /* Initially hide the chat box off-screen */
            transition: transform 0.3s ease; /* Smooth transition for pop-up effect */
        }

            .chat-box.open {
                transform: translateY(0); /* Bring the chat box into view */
            }

        .chat-message {
            margin-bottom: 10px;
        }

            .chat-message .sender {
                font-weight: bold;
                color: #333;
            }

            .chat-message .timestamp {
                font-size: 0.8em;
                color: #999;
            }

        .input-box {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 20px;
        }

            .input-box input[type="text"] {
                flex: 1;
                padding: 10px;
                margin-right: 10px;
                border: 1px solid #ccc;
                border-radius: 5px;
                font-size: 16px; /* Adjust font size */
            }

            .input-box button {
                padding: 10px 20px;
                background-color: #e74c3c;
                color: #fff;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                font-size: 16px; /* Adjust font size */
            }

                .input-box button:hover {
                    background-color: #c0392b;
                }
    </style>
</head>
<body>

    <header class="hero">
        <nav>
            <ul>
                <li><a href="#">Home</a></li>
                <li><a href="Class/Index">Classes</a></li>
                <li><a href="Gym/Index">Employees</a></li>
                <li><a href="Contact/Index">Contact</a></li>
            </ul>
        </nav>

        <div class="hero-content">
            <h1>Welcome</h1>
            <p>Your journey to a stronger, healthier you starts here.</p>
            <a href="#" class="button">Get Started</a>
        </div>
    </header>

    <section class="about">
        <div class="about-content">
            <h2>About Us</h2>
            <p>
                At Cutting Edge Gym, we're committed to pushing boundaries and achieving fitness excellence. Join us and
                discover the difference.
            </p>
        </div>
    </section>

    <footer>
        <p>&copy; 2024 Cutting Edge Gym</p>
    </footer>

    <!-- Chat box section -->
    <div class="chat-box" id="chatBox">
        <div class="chat-message">
            <span class="sender">John:</span>
            <span>
                Hello! If you have any questions, please click on the Contact section. We will get back to you as soon as
                possible.
            </span>
            <span class="timestamp">12:30 PM</span>
        </div>
        <!-- Add more chat messages here -->
    </div>

    <!-- Chat input form -->
    <form class="input-box" onsubmit="return sendMessage();">
        <input type="text" id="chatInput" placeholder="Type your message...">
        <button type="submit">Send</button>
    </form>

    <script>// Function to display chat box after delay
        window.onload = function () {
            setTimeout(function () {
                document.getElementById('chatBox').classList.add('open');
            }, 2000); // Adjust delay (in milliseconds) for 2-3 seconds delay
        };

        // Function to send message and display in chat box
        function sendMessage() {
            var input = document.getElementById('chatInput').value;
            if (input.trim() !== '') {
                var chatBox = document.getElementById('chatBox');
                var newMessage = document.createElement('div');
                newMessage.className = 'chat-message';
                newMessage.innerHTML = '<span class="sender">You:</span><span>' + input + '</span><span class="timestamp">' + getCurrentTime() + '</span>';
                chatBox.appendChild(newMessage);
                document.getElementById('chatInput').value = ''; // Clear input field after sending message
            }
            return false; // Prevent form submission
        }

        // Function to get current time in HH:mm format
        function getCurrentTime() {
            var now = new Date();
            var hours = now.getHours().toString().padStart(2, '0');
            var minutes = now.getMinutes().toString().padStart(2, '0');
            return hours + ':' + minutes;
        }</script>

</body>
</html>





